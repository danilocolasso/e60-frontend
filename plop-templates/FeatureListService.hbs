import api from '@/services/api'
import { {{pascalCase featureName}} } from '@/types/{{kebabCase featureName}}'
import { PaginatedPayload } from '@/types/PaginatedPayload'
import { PaginatedResponse } from '@/types/PaginatedResponse'

export interface {{pascalCase featureName}}ListPayload extends PaginatedPayload {}

export interface {{pascalCase featureName}}ListResponse extends PaginatedResponse<{{pascalCase featureName}}> {}

export const {{camelCase featureName}}ListService = async (
  payload: {{pascalCase featureName}}ListPayload,
): Promise<{{pascalCase featureName}}ListResponse> => {
  const response = await api.get('/{{routeName}}', { params: payload })
  return response.data
}
