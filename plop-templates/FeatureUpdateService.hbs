import { {{pascalCase featureName}}UpdatePayload } from '@/schemas/{{kebab featureName}}/{{camelCase featureName}}UpdateSchema'
import api from '@/services/api'
import { {{pascalCase featureName}} } from '@/types/{{pascalCase featureName}}'

interface {{pascalCase featureName}}UpdateResponse extends {{pascalCase featureName}} {}

export const {{camelCase featureName}}UpdateService = async (
  payload: {{pascalCase featureName}}UpdatePayload,
): Promise<{{pascalCase featureName}}UpdateResponse> => {
  const response = await api.put(`/{{kebabCase featureName}}/${payload.id}`, payload)
  return response.data
}
